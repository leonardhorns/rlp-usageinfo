from setuptools import find_packages, setup


# If you run into problems with Python imports, run the following command:
# python -m pip install -e .
# The command will use this setup.py file to do some magic.

# https://stackoverflow.com/a/72124527
setup(name="bsc2022-usage-info", packages=find_packages())
